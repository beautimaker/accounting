<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xushicao.accounting.dao.mapper.AccountMapper">
    <insert id="insert">
        insert into account (account_no, account_name, account_type, status, balance, create_time, update_time,
                             currency)
        values (#{accountNo}, #{accountName}, #{accountType}, #{status}, #{balance}, now(), now(),
                #{currency})
    </insert>

    <select id="checkStatus" resultType="AccountDO">
        select *
        from account
        where account_no = #{accountNo}
    </select>

    <update id="freeze">
        update account
        set status='F'
        where account_no = #{accountNo}
    </update>

    <update id="unFreeze">
        update account
        set status='N'
        where account_no = #{accountNo}
    </update>
</mapper>