<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xushicao.accounting.dao.mapper.InnerAccountInfoMapper">
    <insert id="insert">
        insert into inner_account_info (account_no, title_code, reconinst, relation_code, relation_inst_id, create_time,
                                        update_time)
        values (#{accountNo}, #{titleCode}, #{reconInst}, #{relationCode}, #{relationInstId}, now(), now())
    </insert>
</mapper>

